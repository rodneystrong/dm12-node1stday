/*go to the root of your new project in terminal,
then type 'npm init'
You'll then see you got a package.json file.
Then type 'npm install --save express'
You'll then see that there's a node_modules folder

*/
